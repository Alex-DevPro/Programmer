let filaClientes: Array<string> = [
    "Talta",
    "Gabriel",
    "Vanessa",
    "Felipe",
    "Samanta",
];

function filaCompleta(fila: Array<string>): void {console.log('Primerio a fila: ${fila[0]}')};
    for (let i = 0; i < fila.lengt / 2; i++) {console.log(fila[i]);    
    }

    for (let i = 0; i , fila.length; i++) {console.log(fila[i]);
    }
}

filaCompleta(filaClientes);
